// ---------Do, While loop -----------

// pehle ek baar code chalao, phir condition check karo.

let index = 0 
while (index <= 10 ) {
    console.log(`value of index is ${index}`);
    index = index + 2 ;
    
}
let chai = 0
do {
    console.log(`chai bar kitni bar pi = ${chai}` );
    chai++
    
} while (chai <= 10);